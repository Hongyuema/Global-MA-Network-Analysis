# Upload note

To add these materials to the existing GitHub repository, upload the folders `R`, `data`, `outputs`, and `docs` to the repository root. The files are organized so that they merge with the existing `Global-MA-Network-Analysis` repository structure.

Recommended commit message:

```text
Add complete OWID-GCO cross-database analysis materials
```

The uploaded data include the source-level input workbooks, processed matching output, one-star Pearson matrix, two-star GDP-adjusted partial-correlation outputs, metadata, plotting scripts and documentation for the OWID-GCO cross-database analysis.
